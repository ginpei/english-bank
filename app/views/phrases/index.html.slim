h1 Phrases

p.right-align
  = link_to 'New Phrase', new_phrase_path, class: 'btn teal'

- @phrases.each do |phrase|
  article.phrase-index-item
    h1.phrase-body = link_to md_line(phrase.body), phrase
    div.phrase-body = md phrase.translation
